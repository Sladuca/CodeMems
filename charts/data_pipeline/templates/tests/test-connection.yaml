apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data_pipeline.fullname" . }}-test-connection"
  labels:
{{ include "data_pipeline.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "data_pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
