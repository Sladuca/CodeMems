apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notes-registry.fullname" . }}-test-connection"
  labels:
{{ include "notes-registry.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "notes-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
