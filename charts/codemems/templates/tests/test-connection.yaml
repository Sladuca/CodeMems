apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codemems.fullname" . }}-test-connection"
  labels:
{{ include "codemems.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "codemems.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
